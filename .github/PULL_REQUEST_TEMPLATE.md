## What & why

<!-- What changes, and why it's worth it. -->

## Checklist

- [ ] No `.env`, API keys, or real credentials
- [ ] Docs use `your-domain` / `you@example.com`, never a real domain
- [ ] Only `ui/app.js` committed under `ui/` (the rest is SDK-generated)
- [ ] Claims in docs are true — I verified them
- [ ] New app? `./tests/run_tests.sh` passes against a fresh domain
